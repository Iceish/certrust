<div class="container">
    @if(!empty($header))
        <div class="container__header">
            {{ $header }}
        </div>
    @endif


    <div class="container__body">
        {{ $body }}
    </div>
</div>
